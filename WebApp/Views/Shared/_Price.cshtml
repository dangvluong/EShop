@model Product
@{
    if (Model.PriceSaleOff is null)
    {
        <p>Giá: <b class="text-danger">@SiteHelper.FormatCurrencyString(Model.Price)</b></p>
    }
    else
    {
        <p>Giá: <del class="text-secondary">@SiteHelper.FormatCurrencyString(Model.Price)</del> <b class="text-danger">@SiteHelper.FormatCurrencyString(Model.PriceSaleOff.Value)</b> </p>
    }
}